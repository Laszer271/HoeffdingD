from setuptools import setup

setup(
    name='hoeff',
    version='0.0.1',
    url='https://github.com/Laszer271/HoeffdingD',
    install_requires=[
        #TODO: fill that out with requirements for the module
    ],
    
    py_modules=['hoeff']
)
